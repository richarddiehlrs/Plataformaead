import React from 'react';

import { Container } from './styles';

const Dashboard: React.FC = () => {
  const oi = 1;
  console.log(oi);

  return (
    <Container><p>Dashboard</p></Container>
  );
};

export default Dashboard;
